- ts 学习的就是类型 类型是核心
- ts 中的类型分类： 内置类型（DOM、 Promise 原始方法）、基础类型、高级类型、自定义类型
- ts 中 ：后面的是类型 =后面的是值
- ts 中一切从安全的角度出发，看能不能赋值 就看安全不安全
- ts 再编写的时候代码是没有执行的，只是做类型标记
- ts 还有自动的类型推到，不用见到变量就写类型，而是推动的不正确，才需要我们自己写类型
